const multiplique = (a: number, b: number): number => {
  return a * b
}

const saudacoes = (nome: string): string => {
  return `Olá ${nome}, como está?`
}

console.log(multiplique(3, 5))
console.log(saudacoes("João"))
